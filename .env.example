# Prenatal Digital SUS - Variáveis de ambiente
# Copie para .env e ajuste conforme necessário: cp .env.example .env

# PostgreSQL
POSTGRES_PASSWORD=password
POSTGRES_PORT=5432

# OAuth2 (prenatal-auth)
CLIENT_ID=myclientid
CLIENT_SECRET=myclientsecret

# SMTP (prenatal-alertas - opcional para envio de e-mails)
# SMTP_EMAIL=
# SMTP_PASSWORD=
