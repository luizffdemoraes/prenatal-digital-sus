# Perfil test: H2 em memória (não usado no smoke test - repositórios são mockados)
spring.datasource.url=jdbc:h2:mem:prenatal_ia_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Webhook n8n - URL fictícia para testes (evita chamadas reais)
app.n8n.webhook-url=http://localhost:9999/webhook/test
